# retired

Scripts no hook, launchd job, PATH entry or other script reaches (crew#69). Kept for history; nothing runs them. The reason each is here is the `RETIRED` dict in `test_incident_crew69_every_script_is_wired.py`.
