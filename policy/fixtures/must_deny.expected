# One substring per rule that policy/fixtures/must_deny.json must trigger.
# `something failed` is not proof that YOUR rule failed it.
is new hand-rolled policy
grew from 146 to 400 lines
implements 9 command refusals in Python, up from 6
is Python under policy/
